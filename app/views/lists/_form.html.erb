<%= render 'shared/errors', obj: @list %>

<%= form_for @list do |f| %>
  <%= f.hidden_field :board_id %>

  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <%= f.submit %>
<% end %>
