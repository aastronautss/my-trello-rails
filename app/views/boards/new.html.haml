.container
  .col-md-4.col-md-offset-4
    %h2 Create a new board

    = bootstrap_form_for @board do |f|
      = f.text_field :name, label: 'Board name'
      = f.submit 'Create Board', class: 'btn btn-primary btn-block'

