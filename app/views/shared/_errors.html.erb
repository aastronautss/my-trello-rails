<% if obj.errors.any? %>
  <div>
    <p>There were some errors:</p>

    <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <li>
          <%= msg %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>
